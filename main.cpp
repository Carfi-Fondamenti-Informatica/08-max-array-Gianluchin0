#include <iostream>
#include "lib.h"
using namespace std;

int main(){
    int N;
    cin >> N;
    float num[N];
    for(int i=0; i<N; i++){
        cin >> num[i];
    }
    cout << funzione(num, D);
    return 0,
}
